spring:
  #datasource:
    ## Uses env var ORACLE_DB_URL. If missing, falls back to the local docker string
    #url: ${TEST_DB_URL:jdbc:oracle:thin:@dreamtechproduct.com:1521:CRADBDocker}
    #username: ${TEST_DB_USER:SysDBA}
    #password: ${TEST_DB_PSWD:SysDBA}
    #driver-class-name: ${TEST_DB_DRIVER:oracle.jdbc.OracleDriver}
  jpa:
    #database-platform: ${TEST_DB_PLATFORM:org.hibernate.dialect.OracleDialect}
    show-sql: false
    hibernate:
      ddl-auto: create-drop    # update   # validate
      format_sql: true